name: Release
# On push to main, when Cargo.toml's version isn't on crates.io yet: build the
# extension for every platform, and only if that succeeds, publish the crate and
# cut both the v<version> crate release and the ext-v<version> extension release
# (binaries attached).
on:
  workflow_dispatch:
  push:
    branches: [main]

concurrency:
  group: release
  cancel-in-progress: false

jobs:
  # Both gates on one runner and one checkout: the matrix is only ever read
  # when a release is happening, and a second job for it would put another
  # runner start and another `cargo run` ahead of a half-hour build.
  should-release:
    runs-on: ubuntu-latest
    outputs:
      release: ${{ steps.check.outputs.release }}
      version: ${{ steps.check.outputs.version }}
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - uses: actions/checkout@v7
      - id: check
        # The workspace version (inherited by the moraine crate) lives in the
        # root Cargo.toml. Release when crates.io doesn't have it yet; a missing
        # crate (404 from the index) also means "release".
        run: |
          version=$(grep -m1 '^version = ' Cargo.toml | sed -E 's/^version = "([^"]+)".*/\1/')
          echo "version=$version" >> "$GITHUB_OUTPUT"
          if curl -sf -H "User-Agent: moraine-release (alex@contour.app)" \
               https://index.crates.io/mo/ra/moraine | grep -q "\"vers\":\"$version\""; then
            echo "release=false" >> "$GITHUB_OUTPUT"
            echo "moraine $version already on crates.io - nothing to release."
          else
            echo "release=true" >> "$GITHUB_OUTPUT"
            echo "moraine $version not on crates.io - release pending."
          fi
      # A C++-ABI extension only loads into the exact DuckDB version stamped
      # in its footer, so every supported release gets its own build. The
      # list lives in .github/duckdb-versions.
      - id: matrix
        if: steps.check.outputs.release == 'true'
        run: echo "matrix=$(cargo run -q -p xtask -- version-matrix)" >> "$GITHUB_OUTPUT"

  build-extension:
    needs: should-release
    if: needs.should-release.outputs.release == 'true'
    strategy:
      fail-fast: false
      matrix:
        duckdb_version: ${{ fromJson(needs.should-release.outputs.matrix) }}
    # Pinned to the primary release; `duckdb_version`, not this ref, selects
    # which DuckDB each matrix entry checks out. `ci_tools_version` has to
    # name this same ref: it decides which distribution matrix the build is
    # generated from, and a matrix from an older ref omits fields this one's
    # jobs read — v1.5.4's carries no macOS runner, which drops both macOS
    # legs. A dropped leg leaves no job, no check run and no annotation: the
    # run goes red with every visible job green, and `publish` is skipped.
    # `cargo xtask check-pins` enforces it.
    uses: duckdb/extension-ci-tools/.github/workflows/_extension_distribution.yml@v1.5.5
    with:
      extension_name: moraine
      duckdb_version: ${{ matrix.duckdb_version }}
      ci_tools_version: v1.5.5
      enable_rust: true
      # moraine's Rust core (tokio, object_store, SlateDB) does not target
      # WebAssembly, and Windows is unverified; the community build excludes
      # the same set.
      exclude_archs: "wasm_mvp;wasm_eh;wasm_threads;windows_amd64;windows_amd64_mingw"
      # The Linux builds run in a container with a ccache directory mounted
      # at /ccache_dir and cached between runs, but nothing points ccache at
      # it, so every release recompiles DuckDB from scratch into a cache
      # that dies with the container. `test_env_variables` is appended to
      # the env file that same container is built with, which is the one
      # hook this workflow gives us to set it.
      test_config: '{"test_env_variables": {"CCACHE_DIR": "/ccache_dir"}}'

  publish:
    needs: [should-release, build-extension]
    if: needs.should-release.outputs.release == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
    env:
      VERSION: ${{ needs.should-release.outputs.version }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          token: ${{ secrets.RELEASE_PLZ_TOKEN }}
      - name: Authenticate git so release-plz can push the tag
        run: |
          git config --global \
            url."https://x-access-token:${{ secrets.RELEASE_PLZ_TOKEN }}@github.com/".insteadOf \
            "https://github.com/"
      - name: Publish crate and create the v<version> release
        uses: release-plz/action@v0.5
        with:
          command: release
        env:
          GITHUB_TOKEN: ${{ secrets.RELEASE_PLZ_TOKEN }}
          CARGO_REGISTRY_TOKEN: ${{ secrets.CARGO_REGISTRY_TOKEN }}
      - name: Publish the ext-v<version> extension release
        uses: ./.github/actions/publish-extension-release
        with:
          tag: ext-v${{ env.VERSION }}
          target: ${{ github.sha }}
          token: ${{ secrets.RELEASE_PLZ_TOKEN }}
